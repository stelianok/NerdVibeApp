import React from 'react';
import { View, Text } from 'react-native';
import styles from './styles';

const { body } = styles;

export default function ProductDetailedOverview() {
  return (
    <View style={body}>
      <Text>Product Detailed Overview</Text>
    </View>
  );
}
