import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 15,
  },
  title: {
    color: '#000',
  },
  itemsContainer: {},
  itemsFlatlist: {},
});

export default styles;
